# 🧑‍💻 E1 - Quantum/ML Lead (Deep Execution & Validation Guide)

## 1. Core Philosophy & Role Definition
As the Quantum Machine Learning (QML) Lead, you are the mathematical heart of the SIH26139 project. Your primary challenge is navigating the intersection of classical deep learning (DenseNet) and noisy intermediate-scale quantum (NISQ) simulation (Qiskit). 

**The inherent problem you are solving:** Deep neural networks output high-dimensional vectors (e.g., 1024 features from DenseNet121). Quantum simulators physically cannot simulate a 1024-qubit circuit (the limit for local RAM simulation is ~24-30 qubits, and we are capping at 8 for performance). Therefore, you must manage the rigid mathematical bottleneck of dimensionality reduction (PCA) without leaking test data, and then map those 8 dimensions into a Quantum Hilbert space using a parameterized quantum circuit (ZZFeatureMap) to evaluate kernel fidelity.

Your AI Agent is highly capable of writing the code, but AI agents are notoriously bad at **hardware intuition** and **mathematical boundaries**. You must supervise the agent's memory management, data isolation, and quantum circuit depth.

---

## 2. Phase 1: Foundation & Data Constraints

### The Objective
Before any models are trained, you must establish the rigid mathematical laws of the pipeline. If a tensor enters the system with shape `(3, 224, 224)` instead of `(1, 3, 224, 224)`, the entire matrix multiplication chain fails.

### What to Instruct Your Agent
You must instruct your agent to create a `src/ml/utils.py` file containing strict validation functions. The agent must hardcode the expected shapes.

### What to Manually Check & Validate
1. **Pytest Coverage:** Open `tests/test_ml_shapes.py` generated by the agent. 
   - *Detail Check:* Does the test specifically pass a tensor of shape `(1024,)` and assert that it crashes? It MUST crash. The required shape for a single image feature vector is `(1, 1024)`, preserving the batch dimension. If the agent allows 1D arrays, it is hallucinating classical ML logic instead of deep learning batch logic.
2. **Type Hinting:** Ensure the agent used `numpy.ndarray` and `torch.Tensor` type hints in `utils.py`. The system must handle both seamlessly.

### Agent Prompts (Phase 1)
**Initialization Prompt:**
> "Agent, I am E1, the Quantum ML Lead. Load your system directives from `E1_Quantum_ML_Lead_AGENT_PROMPT.md`. We are executing Phase 1. Your task is to build `src/ml/utils.py` to enforce strict NCHW and batch dimension tensor shapes: `(1, 3, 224, 224)` for raw images, `(1, 1024)` for DenseNet features, and `(1, 8)` for PCA features. Then write `tests/test_ml_shapes.py`. Assume incoming data might be raw numpy arrays or PyTorch tensors. Build defensive checks for both. Stop when done for my review."

**Correction Prompt (If Agent forgets batch dimension):**
> "You missed the batch dimension in your validation logic. Scikit-learn and Qiskit expect 2D arrays `(n_samples, n_features)`. Even for a single inference image, the shape must be `(1, 1024)`, not `(1024,)`. Rewrite the validation function to strictly enforce `len(shape) == 2` for feature vectors."

---

## 3. Phase 2: Model Training & Quantum Core

### The Objective
You will project the 1024D features down to 8D using Principal Component Analysis (PCA), train a classical RBF Support Vector Machine (SVM) as a baseline, and then compute a Quantum Kernel Matrix to train a Quantum Support Vector Classifier (QSVC).

### What to Manually Check & Validate
1. **Data Leakage in PCA (CRITICAL):**
   - *Detail Check:* Inspect `src/ml/train_pca.py`. Did the agent call `fit_transform` on the entire dataset? If yes, **REJECT IT IMMEDIATELY**. The agent must load *only* `features_train.npy`, call `fit_transform` on it, and then call *only* `transform` on `features_test.npy`. 
   - *Validation:* Check the saved `models/pca.joblib` by writing a quick scratch script: `import joblib; pca = joblib.load('models/pca.joblib'); print(pca.n_samples_)`. This number must equal exactly 80% of your total dataset. If it equals 100%, you have leaked data and invalidated the academic integrity of the project.
2. **Quantum Circuit Feasibility:**
   - *Detail Check:* Look at `src/ml/train_qsvm.py`. The agent should initialize a `ZZFeatureMap`. Check the `reps` (repetitions) argument. If `reps > 2`, the circuit depth is too high for a fast local simulation. Enforce `reps=2`.
   - *Detail Check:* Check the `entanglement` strategy. The agent should use `entanglement='linear'`. If they use `'full'`, the CNOT gate count scales quadratically $O(N^2)$, which will cause the simulator to hang on a laptop CPU.
3. **RAM Monitoring during Kernel Evaluation:**
   - *Detail Check:* The command `qkernel.evaluate(x_vec=X_train)` computes an $N \times N$ matrix. If $N_{train} = 500$, it computes 250,000 quantum circuit fidelities.
   - *Action:* Open Task Manager / Activity Monitor. When you tell the agent to run `train_qsvm.py`, watch your RAM. If it exceeds 90%, kill the terminal process (`Ctrl+C`). Tell the agent to implement batching in the kernel evaluation or subsample the training data for the prototype.

### Agent Prompts (Phase 2)
**Execution Prompt:**
> "Agent, proceed to Phase 2. Ensure absolute data isolation: fit the PCA and StandardScaler *only* on `features_train.npy`. For the Quantum model, use a `ZZFeatureMap` with `feature_dimension=8`, `reps=2`, and `entanglement='linear'` to prevent CNOT explosion. Precompute the training kernel matrix and save it as a numpy array, then fit the `QSVC`. Save all models to `models/`. Do not run the training scripts yet, just write them."

**Correction Prompt (If Simulator Hangs):**
> "The `qkernel.evaluate` call is hanging and consuming too much memory. The dataset is too large for a single blocking call. Refactor `train_qsvm.py` to evaluate the kernel matrix in chunks of 50x50 sub-matrices, saving intermediate results to disk, and then stitch them together into the final N x N array."

---

## 4. Phase 3: Inference Tooling

### The Objective
To wrap the trained `.joblib` models and the pre-computed `.npy` quantum kernel matrix into a highly optimized, singleton Python class (`ModelInference`) that the backend engineer (E2) can mount into the FastAPI web server.

### What to Manually Check & Validate
1. **Live Kernel Evaluation vs Precomputed:**
   - *Detail Check:* In `src/ml/inference.py`, the `predict()` method must compute the quantum fidelity between the *new* incoming image (1x8 vector) and the *entire* training set (N_train x 8 matrix). 
   - *Validation:* The result of `qkernel.evaluate(x_vec=new_image, y_vec=X_train)` must be an array of shape `(1, N_train)`. This is the inference kernel. If the agent tries to evaluate `x_vec=new_image` by itself without `y_vec`, it will return a `1x1` identity matrix (1.0), and the `QSVC` will throw a dimension mismatch error during `.predict()`.
2. **API Payload Compliance:**
   - *Detail Check:* The output of `predict()` must exactly match the JSON schema. It must return a dictionary with classical and quantum confidence scores extracted from `predict_proba()` (or decision function normalization for QSVC).

### Agent Prompts (Phase 3)
**Execution Prompt:**
> "Agent, proceed to Phase 3. Build `src/ml/inference.py`. The `ModelInference` class must load all models into memory in its `__init__` method. In the `predict()` method, remember that for QSVC inference, you must compute the asymmetric kernel vector between the new sample and the loaded training set. Return the confidences in a Python dict."
